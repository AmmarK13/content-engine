{
  "$defs": {
    "ProviderDescriptorV1": {
      "additionalProperties": false,
      "description": "Generic description of whatever implementation handled a stage.\n\nThe pipeline graph never knows concrete vendor names \u2014 it only knows\ncapabilities (e.g. \"voice_provider\"). This descriptor is where the\nconcrete provider identity is recorded, for telemetry/debugging, without\nleaking that identity into the graph or contracts.",
      "properties": {
        "provider": {
          "description": "Capability-level provider identifier, e.g. 'voice_provider'.",
          "title": "Provider",
          "type": "string"
        },
        "model": {
          "description": "Underlying model name used by the provider implementation.",
          "title": "Model",
          "type": "string"
        },
        "version": {
          "description": "Version/tag of the model or provider implementation.",
          "title": "Version",
          "type": "string"
        },
        "capability": {
          "description": "The abstract capability this provider fulfills, e.g. 'script_generation', 'voice_synthesis', 'avatar_render'.",
          "title": "Capability",
          "type": "string"
        },
        "endpoint": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Provider API endpoint or resource identifier, if applicable",
          "title": "Endpoint"
        },
        "cost": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Cost incurred for this stage execution, if known",
          "title": "Cost"
        },
        "latency_ms": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Execution latency in milliseconds, if known",
          "title": "Latency Ms"
        },
        "timestamp": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When this provider was invoked, if known",
          "title": "Timestamp"
        }
      },
      "required": [
        "provider",
        "model",
        "version",
        "capability"
      ],
      "title": "ProviderDescriptorV1",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Telemetry record for a single stage execution.\n\nDeliberately does not duplicate cost/latency/endpoint - those already\nlive on ProviderDescriptorV1, which this record embeds rather than\nre-declaring.",
  "properties": {
    "run_id": {
      "description": "Which pipeline run this stage execution belongs to",
      "title": "Run Id",
      "type": "string"
    },
    "stage_id": {
      "description": "Which stage/gate this record covers, e.g. 'S10'",
      "title": "Stage Id",
      "type": "string"
    },
    "attempt": {
      "description": "1-indexed attempt number for this stage execution",
      "minimum": 1,
      "title": "Attempt",
      "type": "integer"
    },
    "input_hash": {
      "description": "SHA-256 hex digest of this stage's input payload",
      "maxLength": 64,
      "minLength": 64,
      "title": "Input Hash",
      "type": "string"
    },
    "output_hash": {
      "anyOf": [
        {
          "maxLength": 64,
          "minLength": 64,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "SHA-256 hex digest of this stage's output payload, if produced",
      "title": "Output Hash"
    },
    "provider": {
      "$ref": "#/$defs/ProviderDescriptorV1",
      "description": "Which provider handled this stage - cost/latency/endpoint live here"
    },
    "started_at": {
      "description": "UTC timestamp when this stage execution started",
      "format": "date-time",
      "title": "Started At",
      "type": "string"
    },
    "ended_at": {
      "anyOf": [
        {
          "format": "date-time",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "UTC timestamp when this stage execution ended, if finished",
      "title": "Ended At"
    }
  },
  "required": [
    "run_id",
    "stage_id",
    "attempt",
    "input_hash",
    "provider"
  ],
  "title": "StageRunRecordV1",
  "type": "object"
}
